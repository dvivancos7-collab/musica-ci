<?xml version="1.0"?>
<!DOCTYPE module PUBLIC "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN" "https://checkstyle.org/dtds/configuration_1_3.dtd">

<module name="Checker"> <!-- Nodo raiz del documento, define el inicio donde se ponen todas las reglas que comprobara CheckStyle -->
    <property name="severity" value="warning"/> <!-- severity es la propiedad que decide como se manejan las infracciones que checkstyle detecta, en este caso como warnings -->

    <module name="TreeWalker"> <!-- Módulo que recorre todo el codigo java y analiza su sintaxis -->

        <!-- Nomenclatura adecuada -->
        <module name="TypeName">
            <property name="format" value="^[A-Z][a-zA-Z0-9]*$"/>
        </module>

        <module name="MethodName">
            <property name="format" value="^[a-z][a-zA-Z0-9_]*$"/>
        </module>

        <module name="MemberName">
            <property name="format" value="^[a-z][a-zA-Z0-9]*$"/>
        </module>

        <module name="LocalVariableName">
            <property name="format" value="^[a-z][a-zA-Z0-9_]*$"/>
        </module>

        <module name="ParameterName">
            <property name="format" value="^[a-z][a-zA-Z0-9_]*$"/>
        </module>

        <module name="ConstantName">
            <property name="format" value="^[A-Z][A-Z0-9]*(_[A-Z0-9]+)*$"/>
        </module>

        <!-- 
            - Clases con inicial mayuscula permitiendo posteriormente numeros pero no guiones bajos
            - Metodos y variables (de instancia, locales y parámetros de métodos) con inicial minuscula permitiendo posteriormente numeros y guiones bajos
            - Constantes completamente en mayusculas con separacion de palabras con barra baja y permitiendo numeros
        -->

        <!-- Formato adecuado -->
        <module name="Indentation">
            <property name="basicOffset" value="4"/>
            <property name="tabWidth" value="4"/>
        </module>

        <module name="WhitespaceAfter"/>

        <module name="WhitespaceAround"/>

        <module name="EmptyLineSeparator">
            <property name="allowNoEmptyLineBetweenFields" value="false"/>
            <property name="allowMultipleEmptyLines" value="false"/>
        </module>

        <!--
            - La unidad de intentacion (sangria) sera de 4 espacios (1 tabulacion en vscode pero varia dependiendo del editor)
            - Espacio en blanco despues de if, for, while, catch, comas y signos de operacion
            - Una unica linea en blanco entre metodos, atributos de clase y bloques de codigo dentro de un metodo
        -->

        <!-- Calidad del código -->
        <module name="UnusedImports"/>

        <module name="NeedBraces"/>

        <module name="EmptyBlock"/>

        <module name="EmptyCatchBlock"/>

        <module name="ParameterNumber">
            <property name="max" value="6"/>
        </module>

        <module name="MagicNumber">
            <property name="ignoreNumbers" value="-1,0,1"/>
        </module>

        <module name="MethodLength">
            <property name="max" value="50"/>
        </module>

        <!--
            - Prohibido usar importaciones que luego no se usan
            - Obligacion de usar llaves o brackets aunque el if, for o while sea de una sola línea (Lo contrario eleva la posibilidad de errores)
            - Detecta if, while o similar vacios sin contenido
            - Obliga a registrar el error de try/catch
            - Maximo 6 parametros por método ya que puede cargar demasiado el codigo y dificultar su comprension, habria que usarlos con objetos
            - No se pueden usar numeros literales, favorece el uso de constantes nombradas con las que indicar el numero que se pretende usar (facilidad de comprension) a excepcion
            de aquellos usados comunmente como 1 o 0 para los bucles e inicializar integers
            - Metodos con maximo 50 lineas
        -->

    </module>

    <module name="LineLength"> <!-- Este modulo se pone fuera del treewalker porque no necesita un analisis del codigo, es de tipo formato -->
        <property name="max" value="100"/>
    </module>

</module>